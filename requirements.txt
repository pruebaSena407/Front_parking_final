# Este proyecto es un frontend con Vite + React.
# No se necesitan dependencias Python para build en Vercel,
# porque la parte backend está en Supabase.
# Deja vacío o elimina este archivo si no tienes un backend Python.
